To get down
===========

Instructions:

To check tests:

```
$ git clone <repo>

$ npm install

$ npm test
```

To run:

```
$ docker-compose build
$ docker-compose up -d
.... use it!

$ docker-compose down
```
