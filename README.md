To get down
===========

Instructions:

To check tests:

```bash
$ git clone <repo>

$ npm install

$ npm test
```

To run:

```bash
$ docker-compose build
$ docker-compose up -d
.... use it!

$ docker-compose down
```
